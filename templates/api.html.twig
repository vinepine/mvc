{% extends 'base.html.twig' %}

{% block title %}api{% endblock %}

{% block body %}
<div class="main">
    <div class="profile">
        <p>
            <a href="{{ path('quote')}}">Shows random quotes</a> | 
            <a href="{{ path('number')}}">Shows random number</a> | 
            <a href="{{ path('api_deck') }}">Shows sorted Deck</a><br><br>
            <a href="{{ path('api_shuffle')}}">Shows shuffled deck</a> | 
            <a href="{{ path('api_draw')}}">Shows drawn cards </a>
        </p>
    </div>
</div>
{% endblock %}